



---
- name: Create users and home directories
  hosts: all
  tasks: 
     - name: create users
       user:
          name: "{{a}}"
          password: "{{b}}"
          home: "{{c}}"
     - name: create files
       file:
          name: "{{d}}"
          state: "{{e}}"
...
